#include <Model.h>
#include <GLES2/gl2.h>

void Model::Render() {

	// TODO: After rendering the meshes, remember to delete non-needed data

	// For each blend shape based mesh, get interpolated data
	for(int i = 0; blendShapeMeshes.size(); i++) {
		//triMeshStruct &mesh = meshes[i];

	}

}
